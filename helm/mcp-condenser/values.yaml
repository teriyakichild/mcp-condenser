replicaCount: 1

image:
  repository: ghcr.io/logdna/mcp-condenser
  tag: ""  # defaults to appVersion
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

config:
  # Single-upstream mode (set upstreamMcpUrl)
  upstreamMcpUrl: ""  # required unless condenserConfig is set
  upstreamMcpHeaders: ""  # JSON object of headers to send upstream (e.g. '{"Authorization":"Bearer ..."}')
  condenseTools: "*"  # comma-separated tool names, or * for all
  toonOnlyTools: ""   # comma-separated tool names for direct TOON encoding (no preprocessing)
  toonFallback: "true"  # convert unmatched JSON results to TOON by default
  minTokenThreshold: "0"  # skip condensing if original is below this token count (0 = off)
  revertIfLarger: "false"  # keep original response if condensed has more tokens
  maxTokenLimit: "0"  # global default token cap for all tool responses (0 = off)
  toolTokenLimits: ""  # comma-separated tool_name:limit pairs for per-tool overrides
  heuristics: ""  # comma-separated key:bool pairs to toggle condensing heuristics. Valid keys: elide_all_zero, elide_all_null, elide_timestamps, elide_constants, group_tuples (all default to true)
  proxyPort: 9000

  # Multi-upstream mode (set condenserConfig to JSON config content)
  # When set, overrides upstreamMcpUrl and uses the config file for all settings.
  condenserConfig: ""  # JSON string — see README for schema

# Additional environment variables (merged after config-derived vars)
env: {}
  # MY_CUSTOM_VAR: "value"

service:
  type: ClusterIP
  port: 9000

metrics:
  enabled: false
  port: 9090
  serviceMonitor:
    enabled: false
    namespace: ""
    interval: 30s
    scrapeTimeout: 10s
    labels: {}

resources: {}
  # limits:
  #   cpu: 500m
  #   memory: 256Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}
tolerations: []
affinity: {}

podAnnotations: {}
podLabels: {}
