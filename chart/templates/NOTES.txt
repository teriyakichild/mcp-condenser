MCP Condenser Proxy has been deployed.

Get the service URL:
  kubectl get svc {{ include "mcp-condenser.fullname" . }} -n {{ .Release.Namespace }}

To use as an MCP endpoint, point your agent at:
  http://{{ include "mcp-condenser.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}/mcp
